<p id="notice"><%= notice %></p>

<h1>Revenues</h1>

<table>
  <thead>
    <tr>
      <th>Item</th>
      <th>Quantity</th>
      <th>Price per item</th>
       <th>Totalsales</th>
      <th>Percentage</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% running_total = 0 %>
    <% @revenues.each do |revenue| %>
      <tr>
        <td><%= revenue.item %></td>
        <td><%= revenue.quantity %></td>
        <td><%= revenue.price_per_item %></td>
       <%# <td><%= number_to_currency(revenue.quantity * revenue.price_per_item) %> </td>
        <td><% running_totalsales = (revenue.quantity * revenue.price_per_item) %></td>
       
        


        <td><%= link_to 'Show', revenue %></td>
        <td><%= link_to 'Edit', edit_revenue_path(revenue) %></td>
        <td><%= link_to 'Destroy', revenue, method: :delete, data: { confirm: 'Are you sure?' } %></td>

        <% running_total = running_total + (revenue.quantity * revenue.price_per_item) %>
      </tr>
    <% end %>
  </tbody>
</table>


<%= link_to "Add new item", new_revenue_path(@invoice), class: 'pull-right' %>
<br>
 
 <h4 class="pull-right"> Invoice total: <%= running_total %> </h4>
<%= link_to 'New Revenue', new_revenue_path %>
